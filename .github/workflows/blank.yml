name: Ejemplo de Composite Action

on: [push]

jobs:
  say-hello:
    runs-on: ubuntu-latest

    steps:
      - name: Test
        run: |
          echo "Valor: $STEP_MY_VAR"
        env:
          STEP_MY_VAR: Nivel step
