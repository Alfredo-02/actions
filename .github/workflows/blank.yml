name: Ejemplo de Composite Action

on: [push]

jobs:
  say-hello:
    runs-on: ubuntu-latest
    
    env:
      MY_VAR: "Mi variable de entorno prueba"
  
    steps:
      - name: Test
        run: echo "Valor: ${{ env.MY_VAR }}"
